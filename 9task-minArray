let array = [2, 10, 58, 1, 0, 50, 1];

function min(arr) {
    let newArr = arr.sort(function(a, b) {
        if(a > b) return 1;
        if(a == b) return 0;
        if(a < b) return -1;
    })
    let minValue = newArr.shift()
    return minValue;
}

console.log(min(array));